/*
    posts are runs of non masked source pixels
 */
package thump.render;

/**
 *
 * @author mark
 */
public class Postnot {
    // posts are runs of non masked source pixels

    public int		topdelta;	// -1 is the last post in a column
    public int		length; 	// length data bytes follows
    
}
